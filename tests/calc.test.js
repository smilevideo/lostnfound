//Nearest mode:
test('adds 1 + 2 to equal 3', () => {
    expect(1).toEqual(1);
});

//Upper Limit mode:
test('adds 1 + 2 to equal 3', () => {
    expect(1).toEqual(1);
});

//Lower Limit mode:
test('adds 1 + 2 to equal 3', () => {
    expect(1).toEqual(1);
});